name: lucide_icons
description: Local compatibility shim for lucide icon usage in Kanakku.
version: 0.257.0
publish_to: 'none'

environment:
  sdk: ^3.11.5
  flutter: ">=3.38.4"

dependencies:
  flutter:
    sdk: flutter
